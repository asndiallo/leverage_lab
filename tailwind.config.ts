import type { Config } from "tailwindcss";

// Tailwind v4 is CSS-first (see app/globals.css `@theme inline`); this file
// only exists so tooling that expects a config path (shadcn CLI, editors)
// has one to resolve.
const config: Config = {};

export default config;
